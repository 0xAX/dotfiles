LOAD_PATH = -L ~/.emacs.d/magit/lisp
LOAD_PATH += -L ~/.emacs.d/dash.el
# build transient and with-editor:
#
#  $ make lisp
#
LOAD_PATH += -L ~/.emacs.d/magit-deps/transient/lisp
LOAD_PATH += -L ~/.emacs.d/magit-deps/with-editor
LOAD_PATH += -L ~/.emacs.d/magit-deps/libegit2
